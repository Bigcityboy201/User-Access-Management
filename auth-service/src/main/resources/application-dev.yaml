# Spring Boot application configuration for the development environment
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qlnguoidung
    username: postgres
    password: quangtruong1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
